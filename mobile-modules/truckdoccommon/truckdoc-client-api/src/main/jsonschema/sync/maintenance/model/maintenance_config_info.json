{
  "type": "object",
  "description": "Checklist configuration data",
  "properties": {
    "configVersion": {
      "type": "integer",
      "required": true
    },
    "config": {
      "$ref": "../../checklist/model/checklist_config.json"
    }
  }
}